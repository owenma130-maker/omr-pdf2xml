# omr-pdf2xml 运行期依赖
#
# 核心只依赖 PyMuPDF：裁判（自检报告）靠它读 PDF 的文本层与矢量图形。
# PyMuPDF 是 AGPL-3.0，与本项目许可一致。
PyMuPDF>=1.23

# ---- 以下为"完整管线"或研发用途，非核心必需 ----
# 完整管线需要外部的 Audiveris（Java，独立程序，用户自行安装），不在 pip 里。
# 详见 README「安装」一节。
#
# numpy>=1.24            # 图像处理（自研检测层）
# opencv-python>=4.8     # 同上
# music21>=9             # 只用于独立校验输出是否良构
