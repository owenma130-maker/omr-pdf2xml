# 更新记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- GUI 增加**「复用已有 .omr（跳过识别，只重跑导出）」**。识别一本 90 页的谱子
  要 20 分钟以上，导出只要 5 分钟；改了导出逻辑后想再试一次，不该逼用户重跑
  识别。目录里有多个 `.omr` 时取最新的那个。

### 修复

- **一页坏页不再废掉整本书**（用户实测撞到的墙）。Audiveris 的 MusicXML 导出
  是**整本一次性事务**：一本 92 页的谱子里只要有 2 页空白/歪斜，它就
  `Could not export since transcription did not complete successfully` 然后
  `exit 1` —— 90 页好页的成果一个都不给，用户等了 22 分钟只看到
  "★ Audiveris 失败 (exit 1)"。而 Audiveris 的 `.omr` 是**逐页增量存盘**的，
  好页其实都在。
  - `omr_cli.py` / `omr_gui.py` 都改成**先看 `.omr` 再看退出码**：只要
    `.omr` 在就继续导出，并列出读不了的页码。真的连 `.omr` 都没产出时才报错。
  - 导出器跳过没有 `<page>` 的页（Audiveris 对无效页写出的小 XML），
    并在 `stats['skipped_empty_sheets']` 和 warnings 里如实记录，
    不再把它当成"一页没有音符的乐谱"。
  - `.omr` 里单页 `sheet#N.xml` 被截断/为 0 字节、或 `BINARY.png` 读不了时，
    只跳过那一页，不再让整本导出抛异常（`read_sheet_root()` 统一兜底）。

- **大谱子导出的 O(n²) 去重卡死**。重复页检测原先全对全：92 页 = 4186 个页对，
  每对 2 次**整页**膨胀，实测导出跑 12 分钟以上、内存冲到 5.8 GB，
  而这本谱子里一个重复页都没有。
  - 膨胀从 PIL `MaxFilter(7)` 换成可分离的 numpy 取最大值，**单页快 18~20 倍**
    （3508×4962 实测 1789 ms → 99 ms），且逐位等价（有回归测试钉住）。
  - 去重改为只和前面 32 页比（重复页在真实 PDF 里都是局部现象），
    成本从 O(n²) 降到 O(n·32)。已知边界：相隔超过 32 页的重复页不会被发现。
  - 结果：同一本 92 页的谱子，导出 **>600 秒（未跑完）→ 297 秒跑完**。

- GUI 心跳在导出阶段说"Audiveris 在算"是误导 —— 那时 Audiveris 早已退出，
  在算的是导出器。现在按阶段说实话，并明说"大谱子要几分钟"。

### 测试

- 新增 `tests/test_bad_pages.py`：坏页页码识别、空页必须跳过而不是导出、
  膨胀与原 PIL 实现等价、去重窗口有界且仍能识别真重复页、
  **Audiveris 非 0 退出时仍必须导出**、真失败（无 `.omr`）仍必须报错、
  GUI 复用分支的四种情形。测试总数 7 → 19。

## [0.1.0] — 2026-09-12

第一个可发布版本。**定位：OMR 前端 + 独立裁判**，识别层依赖 Audiveris。

### 新增

- **独立裁判** `omr_engine/referee.py`
  - 印刷小节号：数字 PDF 直接读文本层（零误差）；扫描件退到**定向 OCR**
    （实测常数：谱表顶线 − 号的底边 = 48~52 px，只 OCR `[顶线−122, 顶线−38]`）
  - 本页应有小节数：由相邻两页首号相减，**不需要任何图像处理**
  - 多小节休止：认"描边 ≥2pt 且压在该行谱表中间线上"的粗横杠，用相减法定量
  - 逐 system 对齐：纸上印的号 vs 我们输出的号
  - **读不出就说"没有裁判"，不编数字**
- **命令行** `omr check / omr2xml / pdf2xml / eval / status`
- **结构化导出器**：小节切分修正、多小节休止展开、音高/时值/记号导出
- 最小测试（含两条钉住已修 bug 的回归测试）

### 实测

| 材料 | 结果 |
|---|---|
| 单声部扫描件（3 页，源 PDF 含重复页） | 逐 system 首号对齐 **10/10**；Render 100%、拍号守恒 100%、丢音符 0 |
| 数字 PDF · 18 声部总谱 28 页 | **132/132 小节** |

### 已知限制

见 README「已知限制」一节 —— 其中第一条是**"看清谱面"这一步仍在 Audiveris 手里**。
