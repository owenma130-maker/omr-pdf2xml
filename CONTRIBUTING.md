# 参与贡献

## 最重要的一条规矩

**任何结论都要带"复现命令"。** 本项目所有文档里每个数字都应能自己跑出来 ——
这是它和"只给结果的黑箱"的区别，也是它唯一值得维护的地方。

## 开发环境

```bash
git clone <repo>
cd omr-pdf2xml
python -m venv .venv && . .venv/bin/activate     # Windows: .venv\Scripts\activate
pip install -e ".[dev]"
pytest -q
omr --help
```

完整管线（`omr pdf2xml`）还需要外部的 **Audiveris**（Java 程序），见 README。

## 提交前必须过两道

```bash
pytest -q                                   # 1) 测试
python tools/legal_preflight.py --path .    # 2) 法律预检：必须 0 命中
```

第 2 条不是形式主义：这个仓库的工作目录里可能含有大量研究材料
（厂商逆向记录、派生数据、个人路径、第三方语料），而**发布物必须是干净的**。
预检按五类扫：逆向产物 / 第三方克隆 / 凭据 / 个人路径 / 版权素材。

## 改代码时请注意

- **不要引入对某款商业 OMR 软件的运行时依赖或派生数据。**
- 依赖只允许开源许可（本项目是 AGPL-3.0，故 AGPL/GPL 兼容；MIT/Apache 亦可）。
- **改了行为就要说清"哪份材料上变好/变坏"**，并附上复现命令。
- 判据优先用**纸面/PDF 自身**的信息（印刷号、矢量图形），而不是"我们自己的工具说它对了"。

## 报告问题

请附上：PDF 类型（数字导出 / 扫描件）、页数、`omr check` 的输出、
以及你期望看到什么。**如果能附一小段可公开的乐谱**（公版或你自己作的），最好。
